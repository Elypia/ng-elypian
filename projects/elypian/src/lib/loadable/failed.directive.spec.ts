import {FailedDirective} from './failed.directive';

describe('FailedDirective', () => {
  it('should create an instance', () => {
    const directive = new FailedDirective();
    expect(directive).toBeTruthy();
  });
});
