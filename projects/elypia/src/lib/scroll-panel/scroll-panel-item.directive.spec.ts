import {ScrollPanelItemDirective} from './scroll-panel-item.directive';

describe('ScrollPanelItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollPanelItemDirective();
    expect(directive).toBeTruthy();
  });
});
