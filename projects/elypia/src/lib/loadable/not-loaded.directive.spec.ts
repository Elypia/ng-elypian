import {NotLoadedDirective} from './not-loaded.directive';

describe('NotLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new NotLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
